import React from "react";

const Hashtag = ({item}) => {
  return (
    <div>
      <div className=" ">
        <p className="  border-0  rounded-full">
          #{item}
        </p>
      </div>
    </div>
  );
};

export default Hashtag;
